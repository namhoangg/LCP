package com.lcp.markup.repository;

import com.lcp.markup.entity.PriceMarkupDetail;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PriceMarkupDetailRepository extends JpaRepository<PriceMarkupDetail, Long> {
}
