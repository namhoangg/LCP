package com.lcp.quote.repository;

import com.lcp.quote.entity.CargoVolume;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CargoVolumneRepository extends JpaRepository<CargoVolume, Long> {
}
